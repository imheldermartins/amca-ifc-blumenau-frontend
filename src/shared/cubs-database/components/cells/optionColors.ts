/**
 * Classes por cor de option — re-export da FONTE ÚNICA no pacote
 * `cubs-components` (`lib/optionColors`), que o `ColorPicker` também consome.
 * Fica aqui como re-export só para não quebrar os imports internos da lib que
 * já apontavam para este caminho.
 */
export { OPTION_COLOR_CLASSES } from 'cubs-components'
